# Continuous isopycnal mode + initialization
USE_REGRIDDING = True
REGRIDDING_SCHEME = 0 
ADJUSTMENT_IC = 0   ! Initial condition method. Default is to fail.
                    ! 0: z coordinates
                    ! 1: layered isopycnal
                    ! 2: continuous isopycnal
                    ! 3: sigma isopycnal
